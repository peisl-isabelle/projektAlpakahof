// AOS (Animate on Scroll) initialisieren
AOS.init({
    duration: 800,   // Dauer der Animation in ms
    once: false       // Animation wird nicht nur einmal abgespielt, sondern jedes Mal beim Scrollen
});